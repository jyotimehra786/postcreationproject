<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900   text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-blue-600 rounded-md px-3 py-2  " href="/logout">Logout</a>
        </div>
       
    <h3 class="text-3xl  mb-4"><span class="font-light">Hello</span> <%= user.name %> ♥️!</h3>
    <h5 class="mb-5">you can create a new post</h5>
    <form action="/post" method="post">
        <textarea placeholder="what's on your mind" class=" w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" ></textarea>
        <input class="px-3 mt-2 rounded-md w-40 py-2 bg-blue-600 block" type="submit" value="Create new post">
    </form>
           
    <div class="posts mt-20">
            <h3 class="text-zinc-400">your Posts</h3>
        <div class="postcontainer mt-5 ">

            <% user.posts.reverse().forEach(function(post){ %>
              <div class="post mb-4 w-1/3 rounded-md bg-zinc-800 border-2 p-4 border-zinc-700">
                <h4 class="text-blue-500 mb-2">@<%=user.username %></h4>
                <p class="text-sm tracking-tight text-zinc-300"><%= post.content %></p>
                <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                <div class="btns flex mt-2 gap-5">
                    <a class="text-red-500" href="/like/<%= post._id %>">
                        <%= post.likes.indexOf(user._id) == -1? "Like": "Unlike" %>
                    </a>
                    <a class="text-zinc-400" href="/edit/<%= post._id %>">Edit</a>
                     <a class="text-zinc-400" href="/delete/<%= post._id %>">Delete</a>

                </div>
            </div>
           <% })  %>

           

         </div>

       
    </div>

</div>
    
</body>
</html>